#include "LiteCrypto.h"

u64 packet_sign(u8 pub_key, u8 **data, u64 size) {

   return 0;
}

u64 packet_verify(u8 **data, u64 size) {

   return 0;
}

u64 packet_secret_box(u8 **data, u64 size) {

   return 0;
}

u64 packet_open_secret_box(u8 **data, u64 size) {

   return 0;
}
